generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

model USER_INFO {
  USER_ID   String @id @db.VarChar(255)
  USER_NAME String @db.VarChar(255)
}

model USER_PRIVILEGE {
  USER_ID String @id @db.VarChar(255)
  TRIAL   String @default("N") @db.VarChar(16)
}
