import handleEvents from './app';
import { printHistories } from './history/index';
import {
  getPrompt,
  printPrompts,
  removePrompt,
  setPrompt,
} from './prompt/index';

export {
  handleEvents,
  printHistories,
  getPrompt,
  printPrompts,
  removePrompt,
  setPrompt,
};
